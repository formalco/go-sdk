package formal

const (
	DefaultURL = "https://api.formal.ai"

	// defaultAPIVersion is the X-Formal-API-Version header value shipped with
	// this SDK release. Not overridable — bump when the SDK adopts a new API.
	defaultAPIVersion = "2026-01-29"
)
